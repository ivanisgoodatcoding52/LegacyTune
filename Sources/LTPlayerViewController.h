#import <UIKit/UIKit.h>

// Full-screen Now Playing view, presented modally when the mini player
// (see LTRootContainerController) is tapped, rather than being a tab of
// its own — see the note in LTRootContainerController.h for why.
@interface LTPlayerViewController : UIViewController

@end
